import { WidgetConfig } from "./models/config";

export const DEFAULT_WIDGET_CONFIG: WidgetConfig = {
    dataSource: "eea",
    location: null,
    elementId: "aq-widget",
    apiBase: "",
    iconUrl: null,
};
